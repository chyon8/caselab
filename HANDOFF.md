# CaseLab 세션 핸드오프 — 여기부터 읽으세요

> 새 세션 시작 시 이 파일 먼저. 최종 업데이트: **2026-07-19**
> 제품/도메인 기준 문서는 CLAUDE.md §5-1 표 참조(design.md·SCORING_SPEC.md·DATA_SCHEMA.md 등).
> 상세 진단·결정·백로그는 [NEXT_STEPS.md](./NEXT_STEPS.md).

**CaseLab** = 위시켓 검수 매니저용 프로젝트 인텔리전스 대시보드. 목표: ①새 프로젝트 검수 시 과거 유사사례의 리스크·개발지식 활용, ②핸드오프 후 무슨 일이 있었는지(상태·미팅·예산·과업 변경) 추적.

---

## ✅ 현재 완료·가동 중

- **데이터 파이프라인**(Neon + n8n push): projects 6,032건, qna 16,717건, 미팅 녹취(`meetings`), 타임라인 이벤트. `CASELAB_DATA_SOURCE=postgres`.
- **목록 서버 페이지네이션 + 정밀검색(L1)** — trigram, 제목·공고본문·고객사·기술·카테고리, 토큰 AND·필드 OR.
- **타임라인 생애주기 마일스톤 시딩**, 모집/미팅 분리, "계약체결중" 라벨.
- **🆕 개발사 Q&A AI 요약** (gpt-4o-mini) — 4,384/4,384 전량. 노이즈 제거 + 핵심질문/결정/리스크/키워드. 상세보기 요약 블록 + 빈 상태 UI.
- **🆕 L2 유사사례 임베딩** (text-embedding-3-large, 1536d) — 6,032/6,032 전량. 상세보기 "유사 프로젝트" 패널(pgvector 코사인).
- **🆕 공고문 붙여넣기 검색 (L2 표면)** — `POST /api/similar {text}` → gpt-4o-mini 정규화(`src/lib/normalize.ts`, 프롬프트 v2 + 결정적 `cleanPosting()` 후처리) → 즉석 임베딩(`src/lib/embed.ts`) → 공유 벡터코어(`searchSimilarByVector`). 메인 검색에 키워드/공고문 **모드토글**. 3개 도메인 실측(자동매매 81%·Unity 73%·쇼핑몰 72%).
- **🆕 검색 사용성 개선** — 이름 링크로 새 탭(리스트·칸반), 공고문 결과 카드 링크, 상세 back→항상 목록 복귀(`<Link href="/">`), 공고문 검색 상태 AppContext 유지, **필터 초기화**(홈/로고 클릭 + "✕ 필터 초기화" 버튼), 상세 모집일(연도), 유사카드 계약금액.
- **🆕 검색 품질 개선 (2026-07-19, `707bd71`)** — L1 목록·칸반 검색어 있을 때 관련도(필드가중치) 정렬(기존 날짜순이라 본문에만 스친 단어가 제목일치를 덮음), 유사사례 풀(카드·통계·검수팁) 유사도 하한(1위 대비 상대컷, 절대값 아님), 검수팁 자명성 금지 + 현재 공고문 참조해 무관 리스크 배제, qna 요약에 `technicalNotes`(기술적 제약·대안제안) 필드 추가. 상세: NEXT_STEPS "공고문 검색 랭킹" 항목.
- **🆕 검수 팁 상태 구분 (2026-07-19)** — 표본 부족(<5건)일 때 무조건 숨기던 것 → "데이터 부족(N건)" 안내로 표시. 검수 팁 생성 실패(quota 초과 등)가 유사사례 결과·통계까지 같이 죽이던 것 → 별도 try/catch로 분리, 실패 원인을 `reviewTipsError`로 노출. `/api/similar` 전체 실패 시에도 상태코드만 아니라 실제 에러 메시지 노출.
- **🆕 공고문 첨부파일 검색 (2026-07-19)** — 공고문 검색창에 word/pdf/excel/ppt 드래그앤드롭·다중 첨부. `officeparser`로 서버에서 결정적 파싱(AI 비용 0원), xlsx는 시트별 분리(`[시트명]` 헤더). 텍스트 레이어 없는 이미지·스캔 PDF는 tesseract.js OCR로 폴백(15쪽 상한, ~3.5초/쪽). 한글(.hwp)·구형 바이너리(.doc/.xls/.ppt)는 명시적 미지원 안내. **⚠️ Vercel 배포 후 검증 필요** — 아래 블로커 참조.

> 🆕 상세(스크립트·모델·운영)는 CLAUDE 메모리 [[ai-enrichment-pipeline]]·[[search-architecture]] 및 아래 "운영 스크립트".

## 🔜 다음 착수

1. **공고문 검색 랭킹 — 도메인 명사가 유형 신호를 덮어씀** (2026-07-19 진단, 미해결) — "여행상품"→"상품" 한 단어만 바꿔도 결과가 여행 플랫폼 무더기 ↔ 홈페이지 구축 무더기로 통째로 바뀜(실측). 원인: 임베딩 입력(제목+공고문)에서 도메인 명사가 본문 반복으로 벡터를 지배, 유형 신호는 제목 1회뿐. 선택지 A(유형분류+부스트, 추천)/B(요약 재임베딩)/C(이중벡터)/D(RRF) 비교는 NEXT_STEPS "공고문 검색 랭킹" 항목.
2. **유사사례 집계 통계** (SQL만, AI 0원 — 제품 핵심) — 유사사례를 나열이 아니라 묶음 통계로: "계약 성사 X%·취소는 주로 모집단계·모집 평균 N주·계약금액 중앙값 Y". 공고문 결과 + 상세 유사패널 상단 요약 카드. **⚠️ 계약금액은 `dev_scope`별로 분리 필수**(디자인만 vs 개발+디자인+기획 = 금액 성격 다름), 0원 제외, 평균 아닌 중앙값+사분위수. 재료(생애주기 날짜·status·contract_amount) 전부 DB에 있음.
3. **반응형/모바일** — 필터·홈·네비·리스트/칸반 모바일 대응(현재 미디어쿼리 없음). 공고문 검색 모바일 노출 여부 **미정**.
4. **cron 스케줄** (Vercel Cron, 하루 1회) — 신규 유입분 자동 임베딩·qna 재추출. **미설정.** (n8n 동기화 스케줄도 미설정 — 사용자 사내망.) qna 재추출 트리거 = 날짜 아닌 **"qna 개수 변화"** 기준.

## ⚠️ 블로커 / 대기 / 잊지 말 것

- **공고문 첨부파일 OCR — Vercel 배포 후 실전 검증 필요 (2026-07-19)** — 로컬(next dev)에서는 실제 이미지 PDF로 검증 완료(34초/9쪽, 5,890자, 이어지는 유사사례 검색까지 정상 동작). 다만 세 가지가 배포 환경에서 안 풀렸음: ①`maxDuration=300`(`src/app/api/extract-text/route.ts`)은 **Vercel Pro 플랜 필요**(Hobby는 60초 상한이라 OCR 중간에 잘림) ②첫 OCR 실행 시 tesseract 한글 학습데이터를 런타임에 인터넷에서 내려받음(서버리스 콜드스타트마다 반복될 수 있음) ③페이지당 원본 해상도 비트맵(~14MB)을 메모리에 올림 — 여러 파일 동시 처리 시 메모리 상한 주의. 로컬 개발 중 Turbopack이 `officeparser`(tesseract.js 워커 스레드 포함)를 번들에 넣어 워커 로드가 깨지는 문제가 있어 `next.config.ts`에 `serverExternalPackages`로 제외함 — Vercel 프로덕션 빌드(webpack/Turbopack 무관하게 서버리스 함수 트레이싱)에서도 이 설정이 유효한지 배포 후 첫 확인 대상.
- **AI 프롬프트(ⓒAI 필드)** — 리스크태그·이슈로그·미팅요약·SCORING 스코어링은 사용자 검토까지 보류(단 qna 요약·공고문 정규화는 승인·완료). 대기결정 #2.
- **Q&A 유실 346건** — n8n ③→④ 배치 매핑 버그로 진단됨("안 돌려서"가 아님). 내부망 필요. NEXT_STEPS "⓪".
- **계약금액 0원 건 정체** — 운영팀 확인 중. 집계 시 0 제외 예정.
- **정규화 미세 이슈(무해)** — 안 고른 선택옵션이 가끔 불릿으로 새어듦(temp 0인데도). 실신호가 지배해 매칭엔 영향 없음. 조이려면 프롬프트 강화 or 선택옵션 정규식 사전제거.
- **qna `technicalNotes` 재추출 미실행** — 스키마·프롬프트는 배포됐지만(`707bd71`) 기존 4,384건 요약엔 이 필드가 없어 상세보기 "🔧 기술 쟁점" 그룹이 안 보임. `node scripts/extract-qna.mjs 4400` (~$1.2, ~40분, 멱등이라 중단 후 재실행 가능).

## 🔧 운영 스크립트

```bash
node scripts/extract-qna.mjs [N]      # qna 요약 추출 (qna_summary IS NULL만, 멱등)
node scripts/embed-projects.mjs [N]   # 공고문 임베딩 (embedding IS NULL만, 멱등, 429 재시도)
```
둘 다 이미 처리된 건 자동 스킵 → 재실행하면 신규분만. 원본(`timeline_events`)은 불변, 파생만 갱신.

## 작업 규칙

git은 AI가 직접 실행 안 함, 복붙 명령어만(CLAUDE.md §6). 변경 리포트+체크리스트+컨펌 후 커밋. 원격 origin = github.com/chyon8/caselab. dev 서버는 사용자가 3000포트로 띄움.
