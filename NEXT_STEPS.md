# CaseLab 진단·결정·백로그

> 살아있는 결정·미해결 진단·미착수 백로그만 모은 레퍼런스. 세션 시작점은 [HANDOFF.md](./HANDOFF.md).
> 완료된 일회성 백필 실행지침·날짜별 작업로그는 정리(2026-07-17) 시 제거함 — 파이프라인 실행법은 [n8n/](./n8n) 문서에 있음.
> 설계 근거: [DATA_INTEGRATION.md](./DATA_INTEGRATION.md), 원본 스키마: [DATA_SCHEMA.md](./DATA_SCHEMA.md).

---

## 대기 중인 결정

| # | 내용 | 상태 |
|---|---|---|
| 1 | 통화 요약 외부 클라우드 저장 승인 | ✅ 해소 (2026-07-15 "원문까지 받는다") — 단 통화 트랙 자체가 후순위로 확정(아래) |
| 2 | AI 프롬프트 검토 (이슈 추출·리스크 태그·공고문 구조화·요약) | ✅ **해소** (2026-08-11). qna 요약·공고문 정규화 승인·완료 / SCORING 2026-07-22 착수 / 미팅 요약·이슈 추출 2026-08-10 완료(이슈 추출은 `issue_log`가 아니라 `note_extract` 형태로 구현) / **리스크 태그 2026-08-11 완료** — 고정 15종 택소노미, `ai_insights.risk_tags`, 1,583건. 상세는 HANDOFF "리포트 확장" |
| 3 | 임베딩 제공자 선택 | ✅ 해소 (2026-07-17) — OpenAI text-embedding-3-large 확정, L2 완료 |
| 4 | 공고문의 제3자 API 전송 승인 | ✅ 해소 (2026-07-17) — 승인됨, L2 임베딩 완료 |
| 5 | 계약금액 0원 건의 정체 (예: project 154633) | 🟡 **운영팀 확인 중** — 계약금액 집계 시 0은 분모 제외 예정 |
| 6 | **공급난이도 태깅 백필 범위** (2026-08-12) | 🟡 **대기** — 840/5,965 완료. 남은 전량 약 $54 / 2026년 이후만 약 $8(실측 건당 $0.0105). 비용 0인 대안(라벨 전파·키워드 규칙)을 먼저 재볼 것. 상세는 HANDOFF "다음 착수 ①" |
| 7 | **본진 `cancel_reason` 존재 여부** (2026-08-12) | 🟡 **대기** — CaseLab 4,091건 전부 NULL. 본진에 값이 있으면 취소 사유가 LLM 0원·커버리지 100%로 끝난다. 노트 백필보다 먼저 확인 |

---

## ✅ 해소 — Q&A 댓글 유실 (2026-07-20 재측정)

**결론: 유실 없음.** 본진 in-scope qna **17,271건** ≈ Neon `timeline_events` source='qna' **17,272건**(Neon이 +1 — qna는 삭제/수정 동기화가 없어 본진에서 지워진 1건이 Neon에 남은 것으로 추정, 무해). 2026-07-15에 "앞 10개 없고 뒤 2개만"이던 지목 피해자 project 156821도 지금은 15건 전부 온전(앞 댓글 97323·97326·97398 포함). 커서 정상 진행(`...\|97696`, 2026-07-20 00:53 가동).

**어떻게 해소됐나:** 2026-07-15 당시 346건 유실은 실재했으나, 이후 더 이른 커서에서 증분이 재실행되며 멱등 upsert로 빈칸이 메워짐(원본 `timeline_events` 불변, 부작용 없음). 급성 유실 종료.

**남은 것 — 재발방지 하드닝 (백로그, 착수 안 함):** 근본 원인이던 n8n ③(조회)→④(전송) 배치 트렁케이션은 코드로 못 막았고 재발 가능. ③은 배치 전량을 리턴했는데 ④ 전송 시 일부만 실리면, 커서는 배치 max id까지 전진 → 안 실린 행은 skip=0인 채 영구 누락. 수신 라우트([`route.ts`](./src/app/api/sync/timeline/route.ts))·[`cursor.ts`](./src/lib/sync/cursor.ts)는 정상(실측)이나, **본진 id 순서를 몰라 중간 누락을 단독 탐지 불가.** **고칠 방법:** ③의 `data.length`를 ④ payload에 `expected`로 실어 보내고, 라우트가 `rows.length < expected`면 커서를 안 세우게 함 → 조용한 유실이 "감지되는 정지"로 전환. n8n·CaseLab 양쪽 변경.

> **재발 시 즉효 복구:** Neon `sync_state`의 qna `cursor_value`를 백필 시작점(`2024-11-11T00:00:00Z\|0`)으로 되돌리면 증분 워크플로가 LIMIT 200 루프로 전량 재조회 → 멱등 upsert로 빈칸만 메워짐(별도 재적재 워크플로 불필요). 이번 자가 복구도 이 경로.

> ⚠️ ~~어드민 카드의 "댓글"이 매니저 코멘트면 안 뜨는 게 정상(미동기화)~~ — **2026-08-10부터 동기화됨.**

---

## 확정된 결정

### 유사사례 카드 컷 = 상대 마진 대신 바닥값만 (2026-07-20)
카드(사람이 보는 목록/상세패널)는 "1위 대비 마진" 컷을 쓰면 안 됨 — 1위가 이상치(거의 중복 사례)일 때 마진을 아무리 키워도 2위 이하가 통째로 잘림(문서마다 1위가 튀는 정도가 달라 고정 마진으로 못 막는 구조적 결함, 실측 재현: 아티스트 플랫폼 공고 8건→2건). `CARD_CUTOFF`(`src/data/postgres.ts`)를 바닥값(0.5)만 적용으로 변경 — LIMIT 8로 이미 개수 제한되고 유사도가 화면에 찍혀 사람이 거르므로 무관 섞임 비용이 낮다는 논리.
**통계·검수팁 풀은 기존 상대컷(1위 대비 0.06) 유지** — 같은 결함이 있어 poolSize가 1까지 줄어드는 사례를 확인했지만, 사용자가 "지금 결과로 충분"하다고 판단해 보류. **재개 조건:** 1위가 아니라 2위 기준으로 마진을 재는 방식 검토(이상치인 1위가 창을 못 끌어올리게).

### 통화(calls) 트랙 후순위 확정 (2026-07-16)
by-phone 매핑이 구조적으로 부정확 — 파트너·매니저 번호가 여러 프로젝트에 걸치고 한 통화에 여러 프로젝트가 섞임. confidence=high여도 오염 확인(예: project 153821에 무관 통화 다수 혼입). 지급·정산 이슈가 내용 다수인데 검수 매니저 개입 범위 밖. 껍데기 행(빈 summary/transcript) 35.7%(226/633)로 신호 밀도 낮음.
**조치:** UI 제거(`ProjectDetail.tsx`의 CallCard 삭제) — 데이터는 `calls` 테이블에 **보존만**, 다운스트림 사용 금지. 전체 백필 보류. 재개하려면 client 통화 × `meeting_meeting`에서 번호가 프로젝트 하나에만 걸리는 경우로 좁혀 매핑 신뢰도 확보부터.

### 미팅 녹취 vs 통화 = 별개 소스 (2026-07-15)
| | `calls` (by-phone) | `meetings` (/api/meetings/) |
|---|---|---|
| 매칭 | 전화번호 → project_id 추측 | project_id **직접** 매칭 |
| 특징 | confidence 필터 필요, 남 프로젝트 섞임 | 오매핑 없음 |
| 참여자 | 매니저↔클라 or 매니저↔파트너 양자 | 매니저·클라·파트너 **3자 미팅** 전문 |
| 파이프라인 | [`calls_pipeline.md`](./n8n/calls_pipeline.md) | [`meetings_pipeline.md`](./n8n/meetings_pipeline.md) |

미팅 파이프라인은 구현·실행 완료(`meetings` 테이블 적재, ProjectDetail 렌더). `meeting_meeting`에 `client_cell_phone_number`·`partner_cell_phone_number` 있음 — **SELECT 금지.**

### 계약 업무범위 텍스트 미러링 보류 (2026-07-15)
`work_scope`/`work_detail` 미러링 안 함. 대신 특약 숫자(`total_price`·`date_contracted`)와 `milestone_milestone` 예정일/실제일만. 이유: ①최고 신호(필요요소 8축)는 `devScope`에 이미 있음, ②원문은 계약 어드민 링크로 접근 가능, ③원문 70~80%가 계약 공통 보일러플레이트(프로토타입 렌더로 확인), ④회고적 데이터라 실시간 검수 판단에 안 쓰임. 실물은 7~8단 중첩 malformed HTML(순수 텍스트라는 원가정 틀림).
**재개 조건:** 추출 파이프라인 착수 시 첫 타깃은 **"제외 범위"**(공고엔 있었는데 계약서 협의로 빠진 것) — 신호 최고. 나머지 보일러플레이트는 여러 계약이 모여야 자동 제거 가능.
**계약 첨부파일은 파싱 안 함** — `project_projectfile`은 S3 FileField라 n8n서 signed URL 못 뽑고, PDF/HWP 파서는 비용 급증.

### 검수 스코어링/견적/질문 — 로드맵·아키텍처 원칙 (2026-07-22)

**전체 플로우(사용자 확정):** 러프한 고객 의뢰 인풋 → (통화 전) 견적·유사사례·스코어링·확인 질문 → 통화로 요청사항 확정 → 공고 등록. **미팅 녹취와는 완전히 다른 트랙** — 미팅은 다른 매니저가 하는 일이라 정보만 가져오면 되고(수동적), 검수통화는 매니저 본인이 진행하는 능동적 작업.

**단계별(Phase) 순서 — 2026-08-06 재배열:**
| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 견적 + 스코어링 + 질문 (stateless, 저장 안 함) | `/test`로 구조 완료(2026-07-22). **1차 실사용 완료(2026-08-06)** — 질문 합격, 스코어링은 N/A 구조 수정 반영, 견적은 사용자 튜닝 중. **브리핑 패널 추가(2026-08-07)** — 튜닝 중 |
| 1 | **검수통화 녹취 조회·첨부** (n8n 프록시 + 다중선택 UI) | ✅ **완료·가동 중 (2026-08-07)** — 사용자가 n8n 워크플로 구성, 실제 조회 성공 |
| 2 | **공고문 draft 생성** (의뢰 원문 + 선택 녹취 병합) | ✅ **코드 완료 (2026-08-07)** — `draft.ts`+`/api/test-draft`+패널. **실사용 미검증** |
| 3 | `review_session` 저장(CaseLab 소유 테이블) | ✅ **완료 (2026-08-07, `7f01635`)** — 아래 "Phase 3 구현 결과" |
| 4 | 프로젝트 상세에 검수 이력 표시 (공고 등록 후 project_id로 사후 매칭) | 🔜 **미착수 — 이제 가능**(3이 끝남) |

**순서가 바뀐 이유(2026-08-06):** 원래 draft를 1번에 뒀는데, 사용자가 **"공고문은 초기 요구사항 + 통화 녹취를 합쳐서 쓰는 것"**이라고 정정 → 녹취 확보가 draft보다 **먼저**다. 나머지(저장·이력)는 원래 근거 그대로: 공고 등록이 플로우의 마지막이라 project_id가 그제서야 생기고, 그전 단계의 상태는 CaseLab 자체 테이블에 살아야 한다.

**확정된 설계 원칙:**
- **판단(LLM)과 계산(코드) 분리.** 견적의 M/D·QA/PM·10% 버퍼 산수를 LLM에게 직접 시키면 같은 인풋도 금액이 흔들림(실측). `estimate-calc.ts`가 `prompt.md` 단가표를 그대로 결정적으로 계산하고, LLM은 기능 수량(난이도별 개수)·기간만 판단. 스코어링도 같은 원칙 — LLM은 섹션별 confidence만 판단, 총점·게이트(필수 섹션 임계치)는 코드가 계산.
- **질문은 스코어링과 완전히 분리, 유사사례 재료 금지.** 처음엔 스코어링의 부산물(빈 섹션 = 질문)로 만들었으나, 사용자 피드백: "질문은 업무범위 구체화·견적을 위한 것이어야 하고 스코어링에 치우치면 안 됨" + "유사사례/검수팁에서 질문 끌어오면 다른 프로젝트 얘기 섞여 오염됨". → 독립 생성기(`questions.ts`)로 분리, 이 인풋만 보고 판단.
- **게이트 = 필수 섹션 AND, 총점 임계값 아님.** features가 0인데 나머지로 총점이 채워지면 게이트가 뚫려버림. `scoring.ts`의 `SECTIONS[].required` 4개(purpose·features·admin·platform)가 전부 임계 confidence 이상이어야 통과.
- **검수통화(calls) 재개 판단.** 2026-07-16 결정에서 by-phone 자동매핑 오염 문제로 UI 제거·다운스트림 사용 금지했던 그 `calls` 데이터를, 이번엔 **매니저가 고객번호+날짜/시간으로 직접 검색·선택**하는 용도로 재사용하기로 함 — 자동매핑이 아니라 사람이 고르는 거라 오염 문제와 무관하다는 판단. 단, 그때 확인한 **빈 껍데기 35.7%(226/633, summary/transcript 없음)**는 여전히 유효한 제약 — 검색 결과가 성길 수 있음.
- **검수통화 히스토리 = CaseLab 자체 테이블에 저장.** 사용자가 말한 "히스토리 저장"은 본진에 쓰는 게 아니라 CaseLab 소유 `review_session` 테이블(project_id는 nullable, 공고 등록 전엔 null)을 의미 — "완료 리뷰가 DB에 저장 안 됨"(알려진 한계 항목)의 쓰기 라우트 부재도 여기서 같이 풀림.
- **섹션 "해당없음"(N/A) 분리 — 1차 실사용 피드백에서 나온 구조 수정 (2026-08-06).** 사용자가 실제로 돌려본 결과: 질문은 합격, 스코어링은 현실과 어긋남 — `users`(타겟 사용자/규모), `core_problem`(핵심 문제/현재 운영방식), `admin`(관리자 기능)이 "애매하다". 원인은 **12섹션이 항상 다 해당된다고 전제**하고 "얼마나 적혀있나"만 재는 구조라, **"안 적혀있다"와 "이 프로젝트엔 애초에 없다"를 구분하지 못하는 것.** 관리자 백오피스가 필요 없는 건인데 admin=0 → required라서 게이트가 잘못 막히고, 신규 서비스라 현재 운영방식이 없는데 core_problem=0 → 총점이 억울하게 깎였다.
  **적용한 해법:** LLM이 섹션마다 `applicable`을 판정(기본 true, "인풋에 없다"는 false 근거가 아님 — 프로젝트 성격상 항목 자체가 없을 때만 false). 코드는 ①해당없음을 뺀 나머지 가중치로 총점을 **재정규화**(해당없다고 점수 안 깎임), ②해당없는 필수 섹션은 게이트를 막지 않음. **12섹션 목록은 그대로 두고** UI에 "해당없음"으로 표시만 한다(무엇을 왜 뺐는지 매니저가 봐야 하므로). `scoring.ts`의 `assembleScore`(mock도 이 함수를 재사용해 규칙이 갈라지지 않게).
  **★ 해당없음은 프롬프트로 못 막는다 — 검증 가능한 조건으로 코드가 판정한다 (2026-08-06, 실사용에서 두 번 연속 오판).** LLM은 "인풋에 안 적힘"을 계속 해당없음으로 밀어붙였다: 1차로 **예산·일정·산출물**이, 프롬프트를 강화한 2차에도 **웹사이트 개발 건의 관리자 기능**이 해당없음으로 떴다(둘 다 사용자 리포트). 프롬프트 문구는 LLM이 안 지키면 그만이라 애초에 통제수단이 못 됨. → **2겹 코드 가드**:
  ① `SECTIONS[].naAllowed` — 해당없음이 될 수 있는 5개(core_problem·admin·users·integrations·design)만 허용, 나머지 7개(목적·기능·플랫폼·기술스택·예산·일정·산출물)는 LLM이 false를 줘도 무시. 어떤 프로젝트든 존재하는 항목들이라서.
  ② **인용문 대조(`acceptsNa`)** — 해당없음을 주려면 LLM이 "그건 필요 없다"는 **고객 원문을 그대로 인용**(`naEvidence`)해야 하고, 코드가 그 인용문이 인풋에 실재하는지 대조한다(공백·문장부호 무시, 6자 미만 기각). 없는 인용이면 해당없음을 버린다. 즉 **고객이 명시적으로 "필요 없다"고 말하지 않으면 해당없음이 구조적으로 나올 수 없다.**
  **실측 검증 완료** — ⓐ홈페이지 의뢰(관리자 언급 없음)→해당없음 0개·admin conf 0·게이트 정상 차단, ⓑ쇼핑몰(예산·일정·산출물 언급 전무)→셋 다 해당됨 conf 0, ⓒ"관리자 페이지는 필요 없습니다" 명시→관리자 기능만 해당없음.
  **미확정:** 필수 4개(purpose·features·admin·platform)와 `GATE_THRESHOLD=60`은 여전히 미검증값. `users`를 해당없음 허용에 넣은 것도 재검토 여지(내부 툴이어도 규모는 견적에 영향). N/A 도입으로 admin의 압박은 줄었으나, 유형별 필수 목록이 필요한지는 실사용 더 보고 판단.
- **검수팁은 자동 생성 안 함 — 버튼으로 (2026-08-06).** 분석할 때마다 LLM을 치는 게 낭비라는 사용자 판단. `/test`는 유사사례 검색이 만든 `normalized`만 들고 있다가 "검수팁 생성"을 누를 때 `/api/review-tips`를 친다(새로고침 후에도 눌리도록 localStorage에 `normalized` 포함). **공고문 검색 메인 표면(`ProjectList.tsx`)도 같은 방식으로 전환**(2026-08-06) — 검색 응답의 `normalized`를 `listState.postingNormalized`에 보관, 카드·통계 아래 "검수 팁 생성" 버튼. 페이지 이동 후 돌아와도 버튼이 살아 있다(listState는 유지됨).
- **견적은 사용자가 계속 튜닝 (2026-08-06).** 단가·기능수량 감을 잡아가며 조정하겠다는 사용자 방침. 코드 쪽 구조(판단/계산 분리)는 그대로 두고 상수만 손보는 방향.
- **★ 근거 대조는 "이 프로젝트에 대한 주장"에만 건다 — 분야 지식에 걸면 기능이 죽는다 (2026-08-07, 브리핑에서 배움).** 해당없음(`acceptsNa`) 성공에 기대어 브리핑의 **용어 풀이에도 "그 용어가 원문에 있어야 통과"** 가드를 걸었더니, **고객이 말 안 했지만 매니저가 알아야 할 것**(하드웨어 의뢰의 PCB·펌웨어·외형설계)이 **구조적으로 나올 수 없게** 됐다. 두 종류를 구분해야 한다:
  · **프로젝트에 대한 주장**(고객이 뭘 원한다, 해당없음) → 지어내면 위험, **근거 대조 필수**
  · **분야 일반 지식**(이 유형이면 뭐가 따라온다) → 원문에 없는 게 당연, **대조하면 안 됨**. 검수팁과 같은 성격이고, 실패 모드도 "틀린 사실"이 아니라 "뻔한 소리"라 **자명성 금지** 규칙으로 다룬다.
- **브리핑 패널 (2026-08-07 신설, `brief.ts` + `/api/test-brief`).** "원문을 다 안 읽어도 파악되게" — 한 줄 정의 / 핵심 3~5 / **기술적으로 알아야 할 것** / 고객이 원하는 것. 스코어링의 섹션별 "파악된 내용"이 같은 목적이었으나 12조각으로 흩어지고 프레임이 "얼마나 적혀있나"라 **"그래서 뭔데"에 답을 못 해서** 별도 신설. 질문은 안 낸다(`questions.ts` 전담, 중복 금지).
  **실사용 1차 실패와 수정 (2026-08-07):** 초음파 마사지기 의뢰를 넣으니 *"자동 타이머 기능 — 사용자가 설정한 시간에 자동 종료하는 기능"* 처럼 **고객 기능명을 되풀이**했다. 원인 3중: ①**질문 설계 오류가 가장 큼** — "알아야 할 개념"이라 물으면 고객 기능도 답이 되므로, **"만들려면 뭐가 필요한가"**(부품·회로·공정·인증·연동)로 바꿔 **되풀이가 답이 안 되게** 했다 ②`gpt-4o-mini`가 분야 지식+절제에 약함(**모델 상향은 미결** — 비용은 사용자 결정) ③프롬프트만으로 통제되던 유일한 항목이었음.
  **역방향 가드 도입** — `concepts`는 **용어가 인풋에 그대로 있으면 버린다**(`wants`의 근거 대조와 정반대). 인터뷰형 인풋의 선택지가 특히 잘 새어들기 때문. 대가로 "1.3MHz"처럼 고객이 쓴 용어 자체의 풀이는 막히나, 다른 표현으로 쓰면 통과.
- **공고문 draft는 단일 단계로 구현 (2026-08-07).** 기획서의 2단 구조("통화로 확정된 사항" 먼저 추출 → 병합)는 **안 넣었다** — 통화 2~3건이면 한 번에 처리 가능하고 방침이 "일단 만들면서 고친다"라서. 통화별 녹취는 4000자로 컷. 길이·정확도 문제가 실제로 보이면 그때 쪼갠다.
  **섹션 정의는 `repost.ts`의 `REPOST_HEADINGS`·`REPOST_MISSING`을 재사용** — 재배치본(워딩 불변)과 draft를 **나란히 놓고 비교**하는 게 목적이라 양식이 갈라지면 안 된다. **통화가 원문을 이긴다**(통화가 더 나중 정보): 충돌하면 통화 기준, 고객이 "필요 없다"고 한 건 뺀다.
- **localStorage 번들에 버전이 없다 — 필드를 바꾸면 렌더가 터진다 (2026-08-07 실제 발생).** `caselab-test-last`에 저장된 옛 형태가 그대로 복원돼 `brief.concepts`가 undefined로 크래시했다(`terms`→`concepts` 변경 때). **복원 시점에 형태를 검사하는 `revive*` 함수**로 막았다(안 맞으면 복원하지 않고 버림 — 다시 분석하면 채워지므로). **`/test` 결과 타입을 바꿀 때는 반드시 revive를 같이 갱신할 것.** 렌더에 `?.`를 흩뿌리는 대신 복원 한 곳에서만 막는다.
- **견적 산출 방식.** 사용자가 실사용 중인 `prompt.md`(v9.4.0, IT/하드웨어 컨설팅 리포트 프롬프트)의 단가표·공식을 그대로 코드화. R&D 유형은 M/M 범위를 가산해 총액이 범위값(`{low, high}`)으로 나옴 — **R&D 단가 1000만/월은 가정값**(prompt.md에 R&D 전용 단가가 없어 개발 최고난도 L3 단가 준용, `estimate-calc.ts`의 `RND_PRICE` 상수).

---

### 검수 스코어링 개선 3종 — 기획 확정 (2026-08-12, 미착수)

사용자 제안 3건. **착수 순서는 ② → ① → ③ 이고, 이건 취향이 아니라 의존 관계다** — ①의 금액 델타도 ③의 메일 본문도 «기능 단위 견적»을 재료로 쓴다.

**지금 구조가 막고 있는 것 (착수 전 전제):**
- `questions.ts`와 `estimate.ts`가 **서로를 모른다**(병렬 호출, 인풋만 공유). "이 답이 금액을 얼마나 흔드나"를 말할 재료가 없다.
- `qty`가 **기능 이름 없는 숫자 뭉치**다(`dev:{L1:12,L2:8,L3:3}`). 기능별로 쪼갤 방법이 원천적으로 없고, `includedFeatures`(말)와 `qty`(숫자)의 일치는 프롬프트 지시뿐 구조적 보장이 없다.

#### ② 기능별 예산 나누기 — 먼저

`qty`를 **기능(모듈) 단위로 승격**. `features: [{ name, plan:{L1,L2,L3}, design:{…}, dev:{…} }]`, 기존 파트별 `qty`는 그 합으로 파생. **`estimate-calc.ts`의 계산 로직은 안 건드린다**(하위호환) — 기능별 직접비를 뽑는 `calcFeatureCost`만 추가.

- **공통비(QA·PM·버퍼)는 기능에 안분하지 않고 표 하단 별도 행 (사용자 결정 2026-08-12).** 안분하면 합이 총액과 맞아 깔끔하지만 **PM 공수는 선형으로 줄지 않아** "이 기능 빼면 이만큼 절감"이 과장된다. 예산 협의 재료로 쓰는 게 목적이라 거짓 절감액이 더 비싸다.
- **부가 이득:** A↔B/C/D의 기능별 차분을 코드가 뽑는다 → "무엇을 빼서 얼마가 주는가"가 숫자로 검증된다. 지금은 `excludedFeatures`와 `qty` 감소가 어긋나도 아무도 모른다.
- **리스크:** 기능을 몇 개로 쪼갤지 LLM이 흔들린다 → 프롬프트에 «모듈 6~12개» 상한. 총 수량(개발 합계 30~100개) 지시는 유지.

#### ① 질문의 "답에 따른 변동"

질문마다 **왜 흔들리나 + 분기별 예시답변 + 실제 ±금액**. 원칙은 그대로 — **LLM은 "그 답이면 무슨 작업이 붙나"(기능·난이도·개수)만 판단, 금액은 `calcCost(baseline+delta) − calcCost(baseline)`.**

- **정성 서술이 아니라 실제 금액까지 간다 (사용자 결정 2026-08-12).** 스윙 폭(최대분기−최소분기)이 곧 **"무엇을 먼저 물어야 하는가"의 정렬 기준**이 되는 게 핵심 가치다. 정성 등급으론 그 정렬이 안 선다.
- **별도 3번째 호출**(`/api/test-question-impact`, 재료 = 인풋 + 질문 + 견적 baseline). 질문 생성에 합치지 않는 이유: ⓐ Vercel 60초 ⓑ 질문·견적 병렬 로딩 UX 유지 ⓒ 영향 계산이 실패해도 질문은 남는다. 견적 도착 후 배지가 뒤따라 붙는 형태.
- **가드:** 델타 뻥튀기를 코드가 자른다(분기당 상한, 예 dev L3 5개 초과 금지) + 금액에 **기준 견적(A) 대비 %** 병기.
- **다음 단계(범위 밖, Phase B):** 통화 중 매니저가 분기를 클릭 → 견적 실시간 갱신 → 확정 답변을 `review_session`에 저장 → **공고문 draft의 재료로 투입**(지금 draft는 원문+녹취만 먹는다).

#### ③ 견적 안내 메일 초안

**메일 종류는 «견적 안내»로 확정 (사용자 결정 2026-08-12)** — 확인 질문 메일·공고 초안 확인 메일은 후보에서 내려감. ②의 기능별 예산 표 + 옵션 A~D가 그대로 본문 재료다.

- **발송하지 않는다 — 초안 + 복사 버튼까지.** 고객 메일 주소는 CaseLab이 안 갖고 있는 PII이고, read-only 분석 서비스 원칙과 결이 같다. Gmail·n8n 발송 연동은 별도 결정 사항.
- 미정: 톤·서명 양식, 옵션 4개를 다 보낼지 추천 1개+대안으로 보낼지.

---

### 검수통화 녹취 → 공고문 draft — 구현 완료 (2026-08-07)

기획은 2026-08-06 확정, **Phase 1·2 구현·가동 완료.** 아래는 구현 결과와 실측으로 확정된 사실.

#### 통화 조회 — n8n 프록시 웹훅 (가동 중)

브라우저 → n8n 웹훅 → 통화 API → 브라우저. **CaseLab 서버·DB는 관여하지 않는다.**
노드 4개 구성·값은 [`n8n/calls_proxy_pipeline.md`](./n8n/calls_proxy_pipeline.md). env: `N8N_CALLS_WEBHOOK_URL`.

**실측으로 확정된 것 (2026-08-07, 착수 전 미확인 2건이 여기서 해소):**
- **`member_name`은 실명이 맞다** — 구글 로그인 세션의 표시 이름("장수룡")을 그대로 넘기면 조회된다. `managers.ts` 역매핑 불필요.
- **`member_name`과 `phone`은 AND로 걸린다** — 둘 다 보내면 "그 번호와의 통화 중 내가 한 것"만 나온다. **에러가 아니라 0건**이라 화면만 봐선 눈치챌 수 없다. → UI를 **택일 토글**(이름으로 / 번호로)로 만들고 고른 쪽 값만 전송. (이름 칸을 매번 비우게 하는 건 이상한 UX라는 사용자 지적으로 토글이 됨.)
- **통화 종료 후 API 반영 지연은 아직 미측정** — 실사용하며 확인 필요.

**세팅에서 깨지는 지점 2개 (문서에 원인표로 정리됨):**
- **CORS 프리플라이트** — 브라우저가 `POST + application/json`을 보내면 OPTIONS를 먼저 쏘는데 n8n Webhook 노드가 안 받아 실패한다. → **GET + 쿼리스트링**("단순 요청")으로 보내 프리플라이트를 아예 없앴다. 동기화 버튼(`no-cors` 트리거 전용)과 달리 **응답을 읽어야** 하므로 `Respond to Webhook`에 `Access-Control-Allow-Origin` 필수.
- **n8n 표현식 필드에 `=`를 같이 붙여넣으면 깨진다** — `=`는 "이 필드는 표현식"이라는 내부 표기라 UI에는 `{{ … }}`부터 넣어야 한다. 그대로 넣으면 `JSON parameter needs to be valid JSON`.

**PII 검증 완료 (2026-08-07)** — 고객 정보는 **구조적으로** DB에 못 들어간다. 3중:
① `calls` 테이블에 **전화번호·이메일·이름 컬럼 자체가 없다**(전체 마이그레이션에 고객식별 컬럼 0건) ② 신규 경로에 **DB 참조가 0건**(웹훅 URL 라우트는 `process.env`만 읽음). CaseLab 전체 DB 쓰기 지점 7개 중 어느 것도 이 경로에서 호출되지 않고, 녹취가 흘러갈 분석 API 6개도 전부 read-only ③ n8n Code 노드가 `phone`·`member_name`을 잘라 브라우저까지도 안 온다. 화면 표시 전 `scrubPii` 한 번 더.
- 조회 폼에 입력한 이름·번호는 **localStorage에도 안 남는다**(저장 대상 아님).
- ⚠️ **녹취 원문 속 사람 이름은 여전히 못 지운다**(`scrubPii` 한계, 2026-07-13 확정). **Phase 3 저장 설계 시 "이름이 든 녹취를 DB에 넣을 것인가"가 별도 결정 사항.**

#### 선택 상태 — 조회 결과에 딸린 것 (저장 안 함)

선택한 통화는 **localStorage에 저장하지 않고**, **새 조회 때마다 초기화**한다. 처음엔 저장했다가, 통화 목록 자체는 저장하지 않으므로 새로고침 후 **목록 없이 "N건 선택됨"만 남는 유령 상태**가 되어 걷어냈다.

#### 공고문 draft (코드 완료, 실사용 미검증)

`draft.ts` + `/api/test-draft` + `/test` 맨 아래 패널(버튼, 통화 선택 시 활성화). 설계는 위 "확정된 설계 원칙"의 draft 항목 참조.

---

### Phase 3 구현 결과 — `review_session` (2026-08-07, `7f01635`)

[`migrations/013_review_session.sql`](./migrations/013_review_session.sql) + [`src/lib/review-session.ts`](./src/lib/review-session.ts) + `/api/review-session`.

착수 시 정하기로 했던 것들의 결론:
1. **녹취 원문은 저장하지 않는다** — `call_id`와 요약만 보관하고, 원문이 필요하면 통화 API로 재조회한다. 원천에 남아 있는 걸 복제하지 않으니 "녹취 속 사람 이름"(`scrubPii`가 못 잡는 알려진 한계) 문제가 애초에 생기지 않는다.
2. **`project_id`에 FK를 걸지 않는다** — 검수 대상은 `status='submitted'`라 CaseLab `projects`(모집 이후만 동기화)에 없다. 사후 매칭용 자리로만 둔다.
3. **한 세션 = 한 행, 덮어쓰기.** 화면의 "새 검수"를 눌러야 새 행이 생긴다 — 분석을 다시 돌릴 때마다 행이 쌓이면 목록이 금방 쓰레기로 찬다.
4. 쓰기 라우트는 세션 쿠키(`verifySession`) 인증. `/api/sync/*`의 `X-CaseLab-Key`는 n8n 전용이라 안 맞는다.

**알려진 한계였던 "완료 리뷰가 DB에 저장 안 됨"이 여기서 같이 해소됐다.**

---

**Phase 0 재개 조건:** `/test`(`src/app/test/`, `src/lib/{scoring,questions,estimate,estimate-calc}.ts`)로 실제 검수 사례를 더 돌려 `GATE_THRESHOLD`·필수 섹션 목록을 확정한다(1차 실사용은 2026-08-06 완료).

---

## 미착수 백로그

### L1 검색·유사사례 컷·검수팁 — 완료 (2026-07-19, `707bd71`)

- L1 키워드 검색이 랭킹 없이 날짜순이라 "유지보수" 검색 시 제목 히트가 상위 15건 중 2건뿐이었음 → 필드 가중치 관련도 점수로 정렬 수정 (총 히트 건수는 그대로, recall 손실 없음).
- 유사사례 풀(카드·통계·검수팁)에 유사도 하한이 없어 무관한 사례가 항상 30건 채워지던 것 → 상대 컷으로 수정. **절대 임계값은 쓰면 안 됨** — 쿼리마다 유사도 스케일이 이동해서 한 쿼리의 30위가 다른 쿼리의 8위보다 높은 경우가 실측됨.
- 검수 팁(`mergeReviewTips`)이 자명한 일반론을 내던 것과, 지금 검수 중인 공고를 안 보고 무관한 리스크를 섞던 것 → 프롬프트에 자명성 금지 규칙 + 공고문을 인자로 전달해 관련성 판단하도록 수정.
- Q&A 요약에 기술적 제약·실현가능성 지적을 담을 필드가 없어 근거가 잘려나가던 것 → `technicalNotes` 필드 추가. **기존 4,384건 재추출은 미실행** (`node scripts/extract-qna.mjs 4400`, ~$1.2).

> **공고문 검색 랭킹(유형 vs 도메인) 항목은 2026-08-10 사용자 지시로 드롭했다.** 재론하지 않는다.

---

### 미팅·노트 추출에서 배운 것 (2026-08-10)

미팅 녹취 추출(`meeting-extract.ts`)과 매니저 노트 추출(`managenote-extract.ts`)을 만들며 실측으로 확인한 것들. **프롬프트로 안 되는 게 무엇인지**가 핵심이다.

**★ LLM은 프롬프트에 쓴 형식 예시를 결과에 그대로 실어 보낸다.** 미팅 추출 1차 실행에서 "❌ 나쁜 예 / ✅ 좋은 예"로 넣은 문장이 **토씨 하나 안 틀리고** 기술 쟁점 항목으로 나왔다(의료기기 홈페이지 건에 없는 결제 PG 얘기). "예시를 베끼지 마라"는 문구를 추가해도 통제수단이 못 된다 — **예시 문장을 상수(`PROMPT_EXAMPLES`)로 두고 출력에서 코드가 걷어낸다.** 부분 인용도 잡히도록 포함관계로 비교한다. 노트 추출에도 같은 가드를 처음부터 넣었다.

**★ "주체를 밝혀라"고 시키면 사람 실명이 나온다.** 미팅 후속조치가 `"최진: 정리된 요구 사항을…"`처럼 위시켓 매니저 실명으로 나왔다. 녹취 원문의 이름은 `scrubPii`가 못 잡는 알려진 한계지만(2026-07-13), **우리가 새로 만드는 파생 필드까지 이름을 실어 나를 이유는 없다.** → 주체가 역할(클라이언트/파트너/매니저)이 아니면 `담당자:`로 치환. 전량 실행 결과 후속조치 448건 중 4건에서 실제로 작동했다.
  · 역할 정의를 프롬프트에 명시해도 **주체 오배정(매니저 일을 파트너로 적음)은 `gpt-4o-mini`로는 안 잡힌다.** 사용자 판단으로 그대로 두고 실사용에서 보기로 함. 거슬리면 다음 레버는 모델 상향.

**★ 화이트리스트 필터만으로는 자동생성 문구를 못 거른다.** `management_managenote`는 `note_type='memo' AND flag IN ('normal','notice')`로 좁혀도 **시스템이 같은 값으로 써넣은 자동 문구가 63% 섞여 온다**(실측: 첫 200건 중 125건). 4패턴이 전부라 `NOT LIKE`로 본진 조회 단계에서 잘라낸다 — 적재량 12.9만 → 4.8만.

**★ 나머지 노이즈는 정규식으로 못 자른다 — 그래서 순서가 강제된다.** 자동생성분을 걷어낸 뒤에도 **72%가 미팅 일정 조율·고객에게 보낼 카톡 원문·발송 기록**이다. 사람이 쓴 자유 텍스트라 패턴이 없어 AI 판정이 필요하고, 따라서 **적재 → AI 판정 → 표시 제외** 순서를 피할 수 없다. 원문은 지우지 않고 접어서 보관한다(AI 오판 시 확인 경로).

**★ 추출 단위는 데이터가 정한다.** 미팅은 **녹취 1건 = 추출 1건**(`meetings.ai_extract` 컬럼)이고, 노트는 **프로젝트 1건 = 추출 1건**(`ai_insights.note_extract`)이다. 노트는 건당 평균 131자 단편이라 혼자선 뜻이 안 통하고, 시간순으로 묶어야 "왜 취소됐나"가 나온다. `ai_insights`는 `project_id` 유니크라 미팅(프로젝트당 최대 5건)을 담을 수 없다.

**★ 계획한 스키마가 실물과 안 맞으면 스키마를 바꾼다.** 노트 추출은 원래 `{이슈유형, 발생단계, 원인태그, 심각도, 근거문장}`으로 잡혀 있었으나, 실제 노트 809건을 프로젝트 단위로 읽어보니 "이슈"가 거의 없었다. 실제 금맥은 **①왜 계약/취소됐나 ②공고엔 없던 클라이언트 조건 ③금액·과업 변경 ④파트너 탈락 사유**였고, 이건 HANDOFF의 목표②("핸드오프 후 무슨 일이 있었는지 추적")와 정확히 맞는다. 최종 6종으로 확정:
  `outcome` / `clientRequirements` / `scopeChanges` / `partnerFeedback` / `riskSignals` / `otherNotes`
  · **`otherNotes`는 사용자 요청으로 넣은 캐치올.** 집계는 안 되지만 실제로 값을 냈다(646개 중 상당수). "5개 중 하나에 들어갈 수 있으면 반드시 거기로"라는 규칙 + 개수 상한(4)으로 다른 필드를 빨아먹지 않게 막는다.
  · 기존 `ai_insights.issue_log` 컬럼은 **재사용하지 않았다** — `{type,date,src,text}` 엔트리 배열을 가정하고 만들어졌고 UI도 그렇게 읽는다(현재 mock 전용, 실데이터 0건). 형태가 아예 달라 `note_extract`를 새로 팠다.

**★ 긴 원문은 자르지 말고 쪼갠다.** 미팅 녹취 중앙값이 28,400자, 최장 165,441자다. 60,000자를 넘으면 **줄 경계로** 나눠 조각별 추출 후 병합하고, 병합 시 **뒤 조각이 앞을 이긴다**(미팅이 진행되며 결정이 바뀌므로). 줄 중간에서 자르면 양쪽 다 문맥을 잃는다.

**★ 저장은 한 테이블에, 표시는 갈라서.** 매니저 노트를 `timeline_events`에 `source='managenote'`로 넣은 건 동기화 배관(커서·수신 라우트·skip 재시도·`scrubPii`)이 이미 `source` 단위로 깔려 있기 때문이다. 별도 테이블이면 그걸 전부 복제해야 하는데, **복제한 커서가 어긋나 데이터가 조용히 새는 게 이미 겪은 사고**(qna 346건 유실)다.
  · 단 **화면 타임라인에는 넣지 않는다** — 생애주기 축(검수→모집→계약→진행→완료)의 사건이 아니라 "그 사이에 매니저가 뭘 했나"라, 24줄이 끼면 마일스톤이 파묻힌다. qna가 이미 같은 처리다. 실제로 `postgres.ts`의 타임라인 필터에서 `managenote`가 빠져 있던 게 버그였다.

---

### 🐛 `timeline_events.meta` 이중 인코딩 — 해소 (2026-08-10, 마이그레이션 015)

**증상:** `meta->>'by'`가 항상 NULL. `jsonb_typeof(meta) = 'string'`이 qna 18,494건·meeting 9,685건 전부.

**원인:** 본진 SQL의 `JSON_OBJECT(...)`가 n8n MySQL 노드를 거치며 **JSON 문자열**로 도착하는데, 수신 라우트가 그걸 한 번 더 `JSON.stringify` 해서 jsonb에 문자열 타입 값이 들어갔다.

**눈에 띄지 않았던 이유 — 조용히 틀린 값이 나왔다:**
- **Q&A "비공개" 배지가 한 번도 안 떴다.** `isPrivate`가 항상 false였는데, 풀어보니 **18,494건 중 16,939건(92%)이 비공개**다(문서의 "88%"와 일치).
- Q&A 작성자가 빈 값, `refresh.ts`의 qna 요약도 `by`를 못 실었다.

**조치:** 라우트에 `metaJson()` — 문자열로 온 JSON은 그대로 통과시키고, **JSON이 아닌 문자열만** 감싼다(한 건 때문에 배치 전체가 500 나고 커서가 멈추면 안 되므로). 기존 행은 `UPDATE ... SET meta = (meta #>> '{}')::jsonb WHERE jsonb_typeof(meta) = 'string'`로 해제(멱등).

> **교훈:** 파이프라인 경계에서 타입이 바뀌는 건 예외가 아니라 기본값이다. `JSON.stringify`를 무조건 부르는 코드는 상대가 이미 문자열일 가능성을 봐야 한다.

---

### 임베딩 인덱스는 두 개 — 뭉치면 둘 다 안 됨
| | **A — 프로젝트 (유사사례/L2)** | **B — 청크 (RAG/L3)** |
|---|---|---|
| 단위 | 프로젝트 1건 = 벡터 1개 | 텍스트 조각 = 벡터 1개 |
| 텍스트 | 제목+공고문 | Q&A 요약·매니저 노트·녹취 요약 |
| 저장 | `projects.embedding` | **새 청크 테이블 필요** |
| 상태 | ✅ **완료 (2026-07-17)** | 대기 — L3 어시스턴트 착수 시 |

"이 PG 썼을 때 뭐가 문제였나"의 답은 프로젝트 레코드가 아니라 **댓글 본문 안에** 있다(→B). "비슷한 프로젝트"는 청크로 못 찾는다(→A).

> ⚠️ **A(프로젝트 벡터)로 "유형"을 만들려다 실패했다 (2026-08-12).** k-means(k=16) 결과가 «쇼핑몰 구축»·«매칭 플랫폼» 같은 대분류라, "사방넷·파스토(3PL) 통합관리 초기 설정"이 «쇼핑몰 구축»에 묻혔다. **공고문 전체를 한 벡터로 누르면 "무슨 분야인가"가 지배해서 카테고리를 재발명하게 된다.** 공고 안의 한 문장짜리 신호(상주 필수, STM32, Nexacro)는 평균에 묻힌다 — 그게 정확히 B(청크)가 필요한 이유다. 폐기 경위는 HANDOFF "리포트 확장".

**재료가 크게 좋아졌다 (2026-08-10).** 전에는 qna 요약뿐이었는데 지금은:
| 소스 | 건수 | 성격 |
|---|---|---|
| `ai_insights.qna_summary` | 4,535 | 개발사가 짚은 기술 제약·리스크 |
| `ai_insights.note_extract` | 646 | 왜 계약/취소됐나, 클라이언트 조건, 파트너 탈락 사유 |
| `meetings.ai_extract` | 133 | 3자 미팅에서 확정된 것·미해결 쟁점 |

셋 다 **노이즈를 걷어낸 고정 스키마 추출**이라 원본 노이즈 텍스트보다 훨씬 나은 임베딩 소스다. "이런 클라이언트 유형은 뭘 요구하더라", "이런 파트너는 왜 탈락하더라" 같은 질문이 이제 답 가능한 범위에 들어왔다.

### 특약(subcontracts) — 계약 숫자만 (2026-07-15 확정)
지금 계약 정보는 `contractAmount` 칩(총액) 하나뿐. 가져올 것(전부 숫자·날짜, AI/파서 불필요):
- `sub_contract_subcontract.total_price`/`date_contracted` — 특약별 금액·체결일
- `milestone_milestone.contract_date_*` vs `date_*` — 차수별 예정 vs 실제(지연 분석)

**같이 고칠 버그 — `agreement_price` 서브쿼리 조건 불일치.** [`projects_incremental.sql`](./n8n/projects_incremental.sql)에서 금액은 `hide=0, date_deleted IS NULL` + `ORDER BY a.id DESC LIMIT 1`로 뽑는데, `has_valid_agreement`는 유효 특약(`is_incomplete_addon=0 AND is_cancel_addon=0`)까지 확인 → 조건 달라 agreement 여러 개면 **0원 껍데기를 고를 수 있음.** 금액을 유효 특약 `SUM(sc.total_price)`로 유도하면 증액 자동 반영 + 과업 팽창률 덤. 집계 시 0/NULL 분모 제외, **평균 아닌 중앙값+사분위수**(계약금액 롱테일).

### 선정 파트너 (싸다 — 새 워크플로 불필요, 사내망 필요)
`agreement_agreement.partner_id` → `partners_partners`의 `grade`(prime/pro/boost)·`rating`·`team_size`·`project_accepted`·`job_slug`. `projects_incremental.sql`에 몇 줄 추가. **왜:** "왜 깨졌나"의 절반은 파트너 쪽 — "팀 1명 파트너 붙은 고액 건 완료율", "신규 vs prime 취소율"이 SQL만으로 나옴. 이게 되면 타임라인 '진행 착수=선정 마커' 한계(진짜 계약체결일·파트너)가 풀림.
**결과물(산출물)은 안 가져옴** — 본진에 산출물 테이블 자체가 없음.

### ✅ 미팅 녹취 AI 추출 — 완료 (2026-08-10)

계획대로 qna 패턴을 재사용해 구현. 133/134건 추출 완료(1건은 92자 껍데기). 상세는 위 "미팅·노트 추출에서 배운 것"과 HANDOFF.

### ✅ 매니저 노트(코멘트) — 완료 (2026-08-10)

2026-07-14 추정치와 실제의 차이:
| | 당시 추정 | 실제 |
|---|---|---|
| 대상 | 129,000건 / 5,993 프로젝트 | **자동생성 63% 제외 후 약 4.8만** |
| 모델·비용 | Haiku 4.5 Batch, ~$25 | **gpt-4o-mini, $1~2** (필터로 입력이 줄어 훨씬 쌈) |
| 스키마 | `{이슈유형, 발생단계, 원인태그, 심각도, 근거문장}` | **6종으로 재설계** — 실물에 "이슈"가 거의 없었다 |

"진짜 병목은 품질·스키마"라는 판단은 맞았다 — 비용은 문제가 안 됐고, 스키마를 실물 보고 다시 짜야 했다. **AI 전 0원 필터**도 예상대로 필요했다(`NOT LIKE` 4패턴이 63%를 걷어냄).

> **관통 원칙:** 날짜·금액·상태·퍼널·계약률은 SQL이 더 정확하고 공짜 — AI 안 씀. AI는 SQL이 못 대는 자연어(노트·Q&A·통화요약·공고문)에만, 그것도 고정 스키마 추출로.

---

## 알려진 한계

- 🟡 **모집 퍼널 "조회수" 원천 없음** — 본진에 view_count 컬럼 없음. 퍼널은 지원→미팅→선정 3단만. (`proposal_count`는 정상 — 지원 시 `date_modified` 갱신 98.9%.)
- 🟡 **매니저 코멘트·Q&A는 수정 미반영** — 두 원천 다 `date_modified` 없어 커서를 `date_created`로 잡음. 한 번 가져온 글의 나중 수정은 안 받음.
- ✅ ~~완료 리뷰가 DB에 저장 안 됨~~ — **해소 (2026-08-07)**, Phase 3 `review_session`에서 쓰기 라우트를 깔며 같이 풀림.
- 🟡 **알림(notifications) 항상 빈 배열** — 본진에 대응 원천 테이블 없음.

> **비공개 Q&A는 그대로 노출** (2026-07-14 확정) — 21,324건 중 88%가 비공개(`status=0`)지만 매니저는 원래 어드민에서 다 보던 것. "비공개" 배지만 달아 맥락 표시.
